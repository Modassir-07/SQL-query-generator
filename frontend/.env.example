# Only needed for LOCAL DEVELOPMENT where frontend (vite) and backend (uvicorn)
# run on different ports. Leave unset in production — nginx reverse-proxies
# /api requests to the backend on the same origin/port (80).
VITE_API_URL=http://localhost:8000
